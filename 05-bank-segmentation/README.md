# 05 – Bank Segmentation (WIP)

KDNuggets #4 — simulated bank via SQL scripts (no CSV in upstream).  
Source: https://github.com/franklinanalytics/Bank-Segmentation-Analysis

Generate CSVs later by running `schema_setup.sql` + `data_generation.sql` in PostgreSQL if this becomes the active build.
